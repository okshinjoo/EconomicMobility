# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

"Empower" (Economic Mobility Project) is a free financial-education website aimed at first-generation, low-income, and immigrant youth. Next.js 16 (App Router) + React 19 + Tailwind CSS v4, all TypeScript. No backend yet — everything is static/client-side.

## Commands

```bash
npm run dev      # dev server at http://localhost:3000 (Turbopack)
npm run build    # production build
npm run start    # serve the production build
npm run lint     # ESLint (eslint-config-next: core-web-vitals + typescript)
```

There is no test framework configured. Verify changes by running `npm run dev` and checking the page, and `npm run build` to catch type errors (the build type-checks the whole tree).

## Architecture

Built routes: `/` (`app/page.tsx`), `/quiz`, `/about`, `/learn` (hub), `/learn/[topic]` (SSG per `TopicId`), `/learn/[topic]/[article]` (SSG, one page per article — see the Articles section for the current count), `/glossary`, `/tools` (hub), and the calculators under it. Calculators are organized into **categories** (`lib/toolsRegistry.ts`): each category has a *main* calculator at `/tools/{budget,debt,savings,college}` plus focused *sub*-calculators nested under it (e.g. `/tools/debt/auto-loan`, `/tools/debt/mortgage`, `/tools/budget/paycheck`, `/tools/savings/compound`, `/tools/savings/retirement`, `/tools/savings/roth-ira`, `/tools/college/student-loan`). Every calc page shows `components/CalcSwitcher.tsx` (pill nav across the category; "soon" items in the registry render disabled). New sub-calc pages use `components/CalcPageShell.tsx`. To add a calculator: add it to `toolsRegistry.ts`, then build its page (it appears in the switcher + hub automatically). Invalid `[topic]`/`[article]` slugs `notFound()`. Also built: `/start-here` (onboarding + FAQ), `/resources` (curated external links + a per-state finder, see below), `/contact` (`components/ContactForm.tsx` — sends in-page via Web3Forms to `Help@economicmobilityproject.org` with an inline confirmation; `mailto` kept only as a fallback link. Same access key as the Ask/Community forms), `/tools/templates` (downloadable spreadsheet templates, see below), and `/ask` (the **"Ask Empower" Q&A** — anonymous question form + curated answered questions, see below). **Every header/footer nav link now resolves** — no 404s.

**Reality Check** (`/tools/budget/reality-check`): the Jump$tart-style "pick the life you want, see the salary it takes" stepper. Data + solver in `lib/realityCheck.ts` (categories/options with national-ballpark monthly costs; `salaryForLifestyle()` binary-searches `estimateTaxes` so the result is a real GROSS salary for a single filer in the chosen state, not a flat "add 22%"), UI in `components/RealityCheckTool.tsx` (Jump\$tart-style GROUPED slides - 15 categories over 5 screens via the `SLIDES` map; the four choice-based slides come first and ALL five type-it-in (`freeEntry`) categories are grouped on the final 'Your own numbers' slide beside the state picker (owner request: manual input only on the last page); prices are deliberately HIDDEN during selection so picks aren't anchored; fun/travel/savings/debts/family are `freeEntry` categories (typed MoneyInput on the slide, stored in `overrides`, because amounts vary too widely for presets); student loans are their own none/average/more-than-average choice (Jump\$tart style), separate from the fill-in debts category; snapshots from before the debt split (a `debt` pick) start fresh instead of resuming; the Home slide has a required big-city-vs-typical toggle (`col`) that switches housing options to their `monthlyHigh` prices; results show the salary as a \$5,000 band plus a 'How we priced your picks' receipt where every category's estimate is a MoneyInput the user can correct - corrections live in `overrides` in the snapshot and recompute the total/band live; snapshot `{picks, stateCode, overrides}` saved to `STORAGE_KEYS.realityCheck` and resumed). Update the option costs occasionally like article figures.

**Downloadable templates** (`/tools/templates`): three `.xlsx` files with live SUM/IFERROR formulas + brand styling (budget, debt payoff tracker, savings goal worksheet). The binaries live in `public/templates/` and are GENERATED by `scripts/build-templates.mjs` (ExcelJS, a devDependency) — run `npm run build:templates`, never hand-edit the `.xlsx`. The card metadata is in `lib/templates.ts` (shared by the page + a promo band on the `/tools` hub). To add one: add an entry to both `build-templates.mjs` and `lib/templates.ts`, then rerun the script.

**Courses** (`/courses`, `/courses/[course]`): focused learning modules in `lib/courses.ts` — each is a curated cross-topic reading path (`articleSlugs`), an auto-derived flashcard deck, and a hand-written 6-question final (`finalQuiz`, each question carries a `sourceSlug` so wrong answers link "Review: <article>"). Flashcards are NEVER hand-written: `getCourseFlashcards()` scans the course's articles with the glossary matcher, so decks stay in sync — to enrich a thin deck, add terms to `lib/glossary.ts` (they also start auto-linking site-wide). The final is gated client-side on all module articles being read (read tracking), with a small "take it anyway" escape hatch; passing (≥`PASS_PERCENT` 80%) awards a local-only badge (`empower:course-badges:v1`, rosette SVG `BadgeMedal` in `components/CourseQuiz.tsx`). Client pieces: `CourseGrid` (hub cards with live progress), `Flashcards` (flip deck), `CourseQuiz`. Adding a course = one entry in `lib/courses.ts` (final-quiz questions must not duplicate the article-level `quiz` stems). Nav entry "Courses" in `lib/nav.ts`.

**Blog** (`/blog`, `/blog/[slug]`): owner-written editorial posts in `lib/blog.ts` — day-to-day money reads (fake-guru spotting, app trends) that don't fit the Learn curriculum. Posts use the same `ArticleBlock[]` format and voice rules as articles and render through `ArticleBody` with a fixed terracotta accent; every post carries a required `image` (`/images/blog/<slug>.jpg`, Unsplash free license, 1600px wide) rendered as the post-page hero, the featured-card banner, and index-row thumbnails — when adding a post, download a matching photo to `public/images/blog/` and VISUALLY verify it with Read before shipping (two memory-recalled Unsplash IDs have turned out to be wildly wrong photos); adding a post to `blogPosts` gives it a page, a spot on the index, and search entries (`blogItems` in `lib/search.ts`). Every post carries `topics: TopicId[]` (its Learn-adjacent subjects) — set it when adding a post. The index is **personalized client-side** (`components/BlogList.tsx`, no numbering): blog pages mount `MarkAsRead` with a namespaced `blog/<slug>` key in the shared read map, and the list scores posts against this device's history (read Learn articles by topic + quiz-selected topics ×2 + past blog reads), featuring the best unread match with a "Picked for you" chip and a "because you've been reading about X" line; already-read posts sink to the bottom with a check. (Index restyled July 2026 to the Base44 blog skeleton in OUR skin, owner call: horizontal featured poster card — photo left, ink divider, sticker chip — then a 3-col photo-card grid with tag + read-time row, and a 'Load more posts' btn-ink button revealing 9 at a time; personalization logic unchanged.) No history → newest-first with "The latest" featured (matches the server render, so no hydration mismatch). The old Q&A page that lived at `/blog` is now `/ask` ("Ask & Answers" in the Community dropdown, alongside Blog).

**Challenges** (`/challenges`, `/challenges/[challenge]`): self-paced action checklists in `lib/challenges.ts` — join (localStorage `empower:challenge-progress:v1`), check off steps (each step may link a guide/tool), finish all steps → local badge (`empower:challenge-badges:v1`, same rosette). The "group" element is each challenge's official team-authored thread post in `lib/communityFeed.ts` (`communityPostId` → `/community#post-<id>`; PostCard articles carry `id="post-<id>"` anchors). COMMENTS support personal likes (same likes map, keyed `c:<commentId>`) and REPLIES (one level deep): approved replies live in `CommunityComment.replies` (curated like everything else - the Web3Forms submission carries `reply_to_comment`/`reply_to_author` so you know where to file it), the visitor's own pending replies render indented under the parent with the review chip (stored in the pending-comments map under composite key `<postId>::<commentId>`), and post comment-counts roll up replies + pendings. Replying to a reply is deliberately not offered until the live system lands. Client pieces: `ChallengeChecklist` (join/steps/completion; toggle reads storage, not React state, to survive rapid clicks), `ChallengeGrid` (hub). The homepage `WelcomeBack` strip doubles as the badge case: it receives `badgeSources` (course + challenge meta) and renders earned medals, newest first. Nav: "Community" is a dropdown (Feed / Challenges / Ask a Question).

**Guided journeys** (`/journey`, `/journey/[goal]` — July 2026, the owner's "personalized journey" directive: the site must point people, not just inform them). `lib/journeys.ts` defines 9 journeys KEYED TO THE SAME GOAL IDS as `GOAL_OPTIONS` in lib/profile.ts (credit/debt/budget/emergency/invest/college/home/retirement/safety) — each is 4 ordered STAGES (milestone phrased as an outcome + one-sentence "why" + 2–4 article slugs + at most one tool/course/challenge/topicQuiz attachment). Journeys SEQUENCE existing content, never teach. `/journey/[goal]` (SSG) resolves items server-side and renders `components/JourneyPath.tsx` — since July 2026 the Base44 roadmap layout (owner-directed direct copy): sticky left trail card (winding START→FINISH SVG, glowing green progress stroke that draws in, milestone nodes with checks, pulsing amber "current" ring — `roadmap-pulse` keyframes in globals.css after ticker-scroll, single forest/amber palette not per-journey color) + right column milestone cards ("You are here" pill, read-only derived checklist rows, compact metas) + a "Next up" box in the trail card. Progress is DERIVED client-side from existing trackers only (read map, `empower:visited-tools:v1` exact-pathname hits, course badges, challenge badges, `topic-quiz:<id>` scores) — nothing new stored, so paths light up from work done anywhere, and everything auto-syncs with accounts. `/journey` renders `components/JourneyIndex.tsx`: profile-goal paths first ("Your goal" chip), then quiz-topic matches (`journeyByTopic` map, also used by QuizResults' "let us walk you through it" links), then in-progress. Memory contract applies: items check off and sink-style dim, never hide. Wired into: nav top-level "Your Path", /start-here step 02, quiz results, ⌘K search (`journeyItems` + a "Your Path" page entry), sitemap. When adding an article/tool/course/challenge that fits a goal, consider slotting it into the journey — and run the resolve check (every slug/courseId/challengeId/topicQuiz must exist; getArticleBySlug returns undefined silently). Adding a journey = new GOAL_OPTIONS entry too, or the "Your goal" chip can never fire. **Courses hub cards** (`components/CourseGrid.tsx`, redesigned July 2026 after owner feedback "too similar / too much to read"): color-banded poster cards — solid course-color header with title + medal, goal line, compact meta, progress; the long `description` renders ONLY on the course page. Don't reintroduce description paragraphs to the cards. 10 courses as of July 2026.

**For Students** (`/students`, July 2026 owner ask: "everything a student needs, right there"; July 13: runs as a MICROSITE — `app/students/layout.tsx` mounts `components/StudentHeader.tsx` (own subnav with CSS-only DROPDOWNS mirroring the main header pattern: Overview / Guides▾ / Deadlines▾ / Scholarships / Tools▾ — every dropdown item resolves to real content; '← Main site' door + AccountButton; mobile = horizontal-scroll subnav, dropdown triggers navigate to their primary href on touch; /students is a real HOMEPAGE: photo hero with live stats + CTAs, four pillar cards, then the bands; anchors #calendar/#shelf/#reminders) and /students/* pages must NOT render the main <Header /> — the layout owns the top): a student hub composed ENTIRELY of existing content — hero, the STUDENT MONEY CALENDAR (`lib/studentCalendar.ts`: six recurring real dates — FAFSA open Oct 1 / scholarship season / state deadlines via /resources / tax day / FAFSA final June 30 / loan-rate reset July 1 — year-tagged like article figures, re-check every July 1), four starter guide rows (STARTER_SLUGS in the page), a stay-on-track column (college journey + paying-for-college course + /life), THE WHOLE STUDENT SHELF (all college-topic guides via getTopicArticles in ReadOrderedGrid rows + STUDENT_LIFE_SLUGS cross-topic picks — both sink read items), the ASSIST.org transfer launcher card (external link + the community-college-transfer-money guide), eight student calculators (STUDENT_TOOLS), and a Students-channel band. Nav: Learn dropdown footer + About dropdown; in search key pages + sitemap. ADD-TO-CALENDAR (July 2026): `lib/ics.ts` builds yearly-recurring all-day VEVENTs (one-week VALARM, next-occurrence dates, RFC5545 folding/escaping) from lib/deadlines ONLY; `components/AddToCalendar.tsx` downloads per-deadline or all-six .ics client-side (no server, no data stored) from the /students calendar cards — studentCalendar entries carry `deadlineId` to map to the registry. EMAIL DEADLINE REMINDERS (July 2026, ships inert — go-live: docs/reminders-setup.md): reminder_subscribers + reminder_sends tables are SERVICE-ROLE ONLY (RLS, no policies); POST /api/reminders subscribes (upsert by email, granular deadline/tips consent), GET /api/reminders/unsubscribe?t= is tokened one-click (same page whether or not the token matched — no probing); the daily cron /api/reminders/send (vercel.json, honors CRON_SECRET) mails each lib/deadlines entry ONCE per year from the 15th of the prior month via Resend batch, recording sends so reruns never double-mail; the /students signup band renders only when RESEND_API_KEY + SUPABASE_SERVICE_ROLE_KEY exist at build. SMS deliberately not built (TCPA/carrier registration burden, minors). 'Tips' is a stored preference with NO automated sender — never bolt on an AI tips generator without an owner call. STUDENT TRACKER (/students/tracker, July 2026, owner ask 'class schedule tracker' reframed money-native): `lib/studentTracker.ts` (one whole-snapshot key empower:student-tracker:v1, syncs like the plan) — courses with units/term/status/grade + transferable checkbox (the STUDENT confirms against ASSIST; we never guess), freeform todos, editable cost-per-unit (default $46 CA). Derived: units banked toward TRANSFER_UNITS (60), unofficial 4.0-scale GPA from graded done courses (P carries units, no GPA weight), transferable vs at-risk units, dollars protected. UI `components/StudentTracker.tsx` (mounted-gated). Deliberately NO schedule fields (days/times = calendar apps' job; add-on if the owner asks twice). SCHOLARSHIP FINDER (/students/scholarships, July 2026): `lib/scholarships.ts` is a CURATED list of 25 real national awards (verified against official sites, VERIFIED_AS_OF shown publicly — re-verify yearly: still running? amount? link?) rendered by `components/ScholarshipFinder.tsx` (stage chips + no-citizenship toggle + lib/fuzzy search, academic-year ordering Aug-first with NO Date() so hydration stays deterministic). Curation bar: no fee-charging or data-harvesting 'scholarships'; deadlines stay 'Typically <month>' since exact dates shift yearly. NATIONAL DB TIER (July 2026, built inert): app/api/scholarships/route.ts proxies CareerOneStop (U.S. Dept of Labor) — Bearer auth, daily fetch cache, defensive field mapping, 503 without CAREERONESTOP_USER_ID/TOKEN; the page section (ScholarshipDbSearch) renders ONLY when those env vars exist at build time, so unconfigured deploys show the curated tier alone. Endpoint path overridable via CAREERONESTOP_SCHOLARSHIP_PATH (their welcome email confirms the exact service path). Go-live steps: docs/scholarship-db-setup.md. Still NO class tracker or ASSIST import (a transfer-credit tracker + sanctioned-ASSIST-access email are the agreed next steps; see docs/plan-builder-spec.md). **My Plan** (`/plan`, July 2026 — plan-builder COMPLETE (sessions 1-3): ProjectionCard does deterministic math from readBudgetSummary() with a light target parse (amount + month name), three states (no snapshot prompt / negative-leftover note / on-track line), always labeled an estimate; wired into nav via the Your Path DROPDOWN (My Plan + Guided paths), the /journey promo band, start-here step 02, a quiz-results CTA band, search + sitemap; plan view groups Now (first 3 undone) / Next / Done with done items sinking; re-plan pre-fills last answers): the five-question intake (`components/PlanApp.tsx`, prefills goal from profile) POSTs to `app/api/plan/route.ts`, which grounds Claude (claude-haiku-4-5) in a KEYED CATALOG of real content (journey stages + fuzzy-matched guides + `lib/deadlines.ts` — the only legal date source) — the model may only echo catalog keys, so hallucinated links are impossible; any AI failure or missing key returns the deterministic journey-flattened fallback (aiComposed:false) so the feature never errors. Plan = whole-snapshot at `empower:my-plan:v1` (syncs as-is). Items auto-check from the shared trackers; deadline/habit items are the ONE place manual checkboxes write state (inside the plan object). Spec + status: docs/plan-builder-spec.md — read it before touching plan behavior. **Life Moments** (`/life`): curated bundles in `lib/moments.ts` (per moment: 3 guide links + tool + course/challenge) — the "start from what's happening" entry point; linked from the Learn dropdown footer + About dropdown. **Glue features:** QuizResults recommends a matching course/challenge from the selected topics (map inside the component); `components/DataBackup.tsx` on /start-here exports/restores every `empower:*` localStorage key as JSON (the no-account portability story). **State highlights:** `lib/stateResources.ts` now carries optional per-state `highlights` (standout programs above the national baseline — free-college promises, state EITCs; 19 states populated July 2026, every URL verified official + operating; absence is deliberate). Yearly re-verify pass: check Oregon Promise (funding pressure) and NY Essential Plan (eligibility tightened mid-2026) first. **Newer tools:** Compare Aid Offers (/tools/college/compare-offers — two award letters side by side, net price = COA − grants, loans counted as loans), Savings Jars (/tools/budget/jars — named sinking-fund tracker, `STORAGE_KEYS.jars`), Net Worth (/tools/budget/net-worth), and `components/CheckBroker.tsx` on /resources (deep-links FINRA BrokerCheck/SEC IAPD). **Deploy readiness:** `app/sitemap.ts` (all routes, BASE currently economicmobilityproject.org — update if the domain differs), `app/robots.ts`, and a custom `app/not-found.tsx` in the C voice.

**Community feed** (`/community`): a moderated Clever-Girl-Finance-style feed with CHANNELS (July 2026, owner request modeled on CGF's community space). `lib/communityFeed.ts` defines the `CHANNELS` registry (28 channels in TRAFFIC ORDER; the rail has a FIND-A-CHANNEL search box filtering by name/tagline (flat results while searching, pinned group hidden); empty channels render a be-the-first nudge instead of a blank feed; sub-channels are HIDDEN from the rail/chips (Reddit model, owner call July 2026) and surface as clickable TAGS on posts instead: cards/rows show the TOP-LEVEL hub chip plus an outlined tag chip when the post lives in a sub (usePostChips); clicking the tag filters to that sub (compact rows use a stopPropagation span so the row doesn't expand), the active-sub state renders as a removable 'Tag: <name> ×' pill (× returns to the parent), and the parent rail row highlights while its sub is active; rail SEARCH still finds subs, the composer picker still offers them (em-dash indented). Subs: Students (financial-aid, student-loans, grad-life), Investing (invest-start, retirement-accounts, crypto-check), Credit & Debt (building-credit, debt-payoff, collections-repair), Making Ends Meet (groceries-food, emergency-fund), Jobs (side-hustles, salary-negotiation), Housing (buying-someday) — subs inherit the parent's color and were deliberately NOT seeded (hub seeds + aggregation cover them; don't team-spam the feed). ONLY the say-hello intro post is pinned (owner call — pinned seed posts were hogging the Best feed; seeds stay, unpinned). In compact view the pinned intro renders as a MiniCard (title + two-line clamped teaser + intro count + 'Read & introduce yourself' CTA) — bigger than a CompactRow, smaller than the full card; click expands — July 2026 owner pass 'categories real people talk in': say-hello / questions ('Ask Anything') / students with SUB-channels financial-aid + student-loans (`parent` field; children indent in the rail, parent filters include their posts via `channelMatches()`, parent counts roll kids up) / investing ('Investing & Retirement') / credit-debt / budgeting-saving ('Making Ends Meet') / banking ('Banks, Cards & Apps') / work-income ('Jobs & Side Hustles') / taxes / housing ('Rent & Housing') / family-firstgen / wins (deliberately LOW in the list) / scams-safety / challenges — id, name, tagline, accent color, lucide icon). Every channel opens with a pinned team seed post linking a relevant guide/tool. Members can PIN channels (pin toggle on rail rows -> `empower:community-pinned-channels:v1`, personal-only like likes, account-synced automatically; pinned channels get their own rail group + lead the mobile chips) and every `CommunityPost` carries a required `channel` plus optional `pinned` (pinned posts sort first in their channel with a Pinned chip). The say-hello channel opens with the pinned 'Start here: introduce yourself' post — intros arrive as comments on it (the CGF pattern); its first comment is the founder's REAL intro (Shinjoo — owner may reword, never replace with invented members). The feed has a CGF-style LEFT RAIL on lg+ (sticky card inside CommunityFeed: Channels group with per-channel lucide icons/colors + live counts, Explore links, compact House-rules card - the page's old right sidebar was folded into it) and falls back to the horizontal chip bar below lg (client filter; default All so the challenges' `/community#post-<id>` deep links keep working). REDDIT INTERACTION MODEL (July 2026): every post has its OWN PAGE at /community/post/[id] (SSG; comments + forms live THERE, not in the feed) with a right rail (About-this-channel card w/ hub icon/tagline/post count + tag note, 'More in <hub>' links, house rules); feed rows/mini-cards are <Link>s to post pages (tag chips preventDefault+stopPropagation); card view shows content + a Reddit action bar (personal Like as ArrowBigUp pill, Comments -> post page, Share = copy-permalink with Copied feedback, Save -> empower:community-saved:v1 personal-only) and a Saved filter toggle sits beside Following; legacy /community#post-<id> deep links client-redirect to the post page; ChallengeChecklist/challenge pages/member pages all link /community/post/<id> directly. PostCard has full (post page) vs feed variants. POST SEARCH box above the sort bar (client-side substring over title/body/author/comment+reply text+authors; composes with the channel filter, Following toggle, and sorts; result-count line and no-match state both name the active channel; clear X). Reddit-style VIEWS + SORT BAR above the feed: compact view is the DEFAULT (CompactRow: chip + title + author/date/comment-count meta; click expands the full PostCard in place with a Collapse control; rows carry the post-<id> anchor and a #post-<id> deep link auto-expands on load), card view = classic full posts; the List/LayoutGrid toggle persists per device (empower:community-view:v1). The composer is hidden behind '+ New post' buttons (sort bar + rail top) with a close X. SORTS: Best (curated file order, pinned first — the default) / Hot ((comments+1)/(ageHours+2)^1.2) / New / Top (most approved comments within Today/Week/Month/Year/All-time windows, newer wins ties). Hot/Top RANKING still runs on approved-comment counts + age only (predates real likes; wiring member-like tallies into sorting is a possible future step — but never fake scores), the composer has a 'Post in' channel picker (follows the active filter; say-hello gets an intro-prompt placeholder) and submissions carry the channel name in the Web3Forms payload; pending local posts store their channel. Adding a channel = one entry in `CHANNELS`. Honesty rule unchanged: no invented members; posts are team-authored or real approved submissions. **Members, Cred, follows (July 2026):** `getMemberIndex()` in lib/communityFeed.ts computes MEMBER PAGES + "Community Cred" from published content only (CRED_POINTS: post 10 / comment 5 / reply 3 — honest by construction, grows when the owner curates a submission in). MEMBER PROFILES (July 2026): profiles carry `bio` (280 cap) + `public_profile` (opt-in, default OFF — privacy-first); the account About tab edits both; member pages render `components/PublicProfileCard.tsx` (lib/liveProfiles.ts fetches by slug — RLS only exposes opted-in rows; profileSlug() there MUST stay in sync with communityFeed.memberSlug); unknown member slugs no longer 404 (live members get a standalone shell, dynamicParams); every member page has a 'Say hi in the community' button to their latest thread — owner decision July 2026: NO private DMs (minors + solo moderation), public replies only, don't add DMs without a fresh owner call. Author names in the feed link to `/community/member/[slug]` (SSG via generateStaticParams, slug = memberSlug(display name) — DISAMBIGUATE first names when curating, e.g. "Maria R.", or profiles merge) showing flairs, Cred + breakdown, member-since, and their published posts/comments; Cred displays as an AVATAR RING, not inline chips (owner: chips too distracting): credRingColor() tiers 10+ bronze #a05f2c / 25+ silver #98a2ad / 50+ gold #e7a33c / 100+ money-green #15624b, applied as a box-shadow ring on feed avatars (tooltip 'Community Cred: N') and the member-page avatar, with a tier legend in the member page's Cred block; the numeric score lives ONLY on member pages (authorMeta computed server-side on the community page carries cred + earned). EARNED FLAIRS (automatic, never picked): computed in getMemberIndex() for non-team members only — Top 1%/10% Poster (published posts) and Top 1%/10% Commenter (comments+replies) via rank <= ceil(N*pct), plus '<Hub> Regular' for >=MIN_EARNED_ACTIVITY contributions rolled up to one top-level hub; ALL tiers additionally require MIN_EARNED_ACTIVITY (10) so early tiny-N percentiles stay honest. Rendered as blackish-grey italic text (text-ink/55 italic) beside names in the feed and under the name on member pages. FOLLOWS are personal-only like likes (`empower:community-follows:v1`, FollowButton on member pages, "Only you can see who you follow") and power the feed's Following filter toggle in the sort bar; public follower counts wait for the live system. `components/CommunityFeed.tsx` ("use client") renders the feed: composer + per-post comment forms submit to the project inbox via the same Web3Forms pattern/key-constant as AskQuestion (inert preview mode until the key is set), and the visitor's own pending posts/comments render immediately from localStorage (`empower:community-posts/comments:v1`) with an "only you can see this until it's reviewed" chip; LIKES (July 2026, owner request): REAL public tallies via the Supabase `likes` table (lib/liveLikes.ts; one row per member per target — post id or `c:<comment id>`; RLS read-all/write-own). `useLiveLikes` in CommunityFeed: signed-in hearts hit the DB with optimistic toggling and tallies render in the action-bar pill, comment hearts, and CompactRow meta; signed-OUT visitors keep the legacy personal-only local heart (`empower:community-likes:v1`) which is NOT counted — tallies are member-only and honest by construction. Requires the likes block at the end of docs/supabase-schema.sql. To publish a submission: move it from the inbox into `communityFeed.ts`. Nav entry "Community" in `lib/nav.ts`. LIVE COMMENTS (July 2026, owner-picked model: APPROVE-FIRST + MEMBERS-ONLY writing): comments/replies on post pages insert into the Supabase `comments` table as status='pending' via lib/liveComments.ts (author snapshot: profile displayName + communityTag/communityFlairs; 4000-char cap). RLS: everyone reads approved, authors read their own pending (the cross-device pending chip), only `moderators`-table members update/delete (schema block at the end of docs/supabase-schema.sql — the owner must RUN IT and insert her user_id into public.moderators). SinglePost fetches live comments per post and MERGES them into the existing render shapes (approved -> post.comments with one-level reply nesting; own pending -> the pending map), so PostCard/CommentItem needed no changes. OWN-CONTENT MARKING (July 2026): your own comments/replies render an outlined amber YouChip beside the name (GitHub-Author pattern), an italic 'me' avatar instead of the initial, and an amber-outlined tinted block around the comment — keep all three in sync if restyling. CommentForm branches: signed-in member -> DB insert ('Posting as <name>'); accountsEnabled but signed out -> members-only sign-in nudge (reading stays open, the POST composer stays anonymous Web3Forms); accountsEnabled=false (no env) -> legacy Web3Forms comment form. AI AUTO-REVIEW (July 2026, owner-picked): member comments POST to app/api/comment/route.ts (Node runtime) which verifies the Supabase JWT, runs the text through Claude (claude-haiku-4-5, one-word SAFE/REVIEW verdict, 8s timeout) and inserts via the SERVICE ROLE key — SAFE publishes instantly (status 'approved'), anything else lands 'pending' in the human queue. FAIL CLOSED everywhere (API error/refusal/timeout/unparseable -> pending). Route is inert (503) until ANTHROPIC_API_KEY + SUPABASE_SERVICE_ROLE_KEY exist in Vercel (server-only, never NEXT_PUBLIC); the client (lib/liveComments.ts addLiveComment) falls back to the direct pending insert on 503/network — so the feature degrades to plain approve-first, never breaks. The moderation prompt targets THIS audience: polite scams/DM-bait, personal contact info, links, directive financial advice — not just profanity; edit it in the route file. Moderation queue at /admin/comments (noindex, unlinked — bookmark it): approve publishes instantly, reject hides forever; access enforced by RLS, not the page — with AI review on, the queue holds only flagged comments. FEED-ROW comment counts still come from curated content only (live counts appear on post pages) — acceptable v1 tradeoff, revisit if it confuses. Curated comments in lib/communityFeed.ts remain the seeds + pre-live history; the Web3Forms comment inbox path is dead for members but posts still arrive by email.

**"Ask a Question"** (`/ask` — moved from `/blog` when the real blog launched): `components/AskQuestion.tsx` is an anonymous submission form (question + optional topic + optional email). It POSTs JSON to **Web3Forms** (`https://api.web3forms.com/submit`) gated on the `WEB3FORMS_ACCESS_KEY` constant — currently **empty**, so it runs in "preview mode" (confirms to the visitor but sends nowhere). To go live: get a free access key at web3forms.com (tied to `Help@economicmobilityproject.org`), paste it into that constant, redeploy. (Submissions then land in that inbox. Swap the fetch for Formspree/Tally if preferred.) The answered Q&As shown below the form are curated content in `lib/communityQuestions.ts` (`CommunityQA[]`, topic-tagged) — add an entry when a good question comes in.

**Accessibility/mobile:** `globals.css` has global `:focus-visible` rings (amber, works on light + dark) and a `prefers-reduced-motion` block. Chart SVGs (`components/Charts.tsx`) are `aria-hidden` since their data is present as text in legends/result rows. All pages verified at 390px with zero horizontal overflow; the calculators' two-column layout stacks below `lg`, the article ToC is `xl`-only. (Capture mobile screenshots via Chrome **CDP `Emulation.setDeviceMetricsOverride`** — a plain `--window-size` screenshot does NOT emulate the mobile viewport and gives false overflow.)

**Calculators** (all `"use client"`, two-column inputs/sticky-results, localStorage-persisted, plain-language insight callouts): Budget Planner, Debt Payoff (`components/DebtCalculator.tsx` + `lib/debtPayoff.ts` — avalanche/snowball simulation; loan amortization via `loanPayment` matches standard calculators to the dollar; the "minimums only" baseline uses a *declining* revolving-card minimum (1% + interest, $25 floor) so credit-card payoff is realistic, while a debt with a `term` amortizes on its fixed schedule; blank minimums are estimated via `estimateMinPayment`), Savings Goal (`components/SavingsCalculator.tsx` + `lib/savingsGoal.ts` — by-deadline or by-amount with compound growth), College Cost (`components/CollegeCalculator.tsx` + `lib/collegeCost.ts` — unmet-need + loan repayment estimate). The three newer ones share presentational primitives + `usd`/`num`/`formatDuration` from `components/CalcUI.tsx`; each persists under its own `STORAGE_KEYS` entry. Data viz is `components/Charts.tsx` (dependency-free SVG): `Donut`, `TrendChart` (balance-over-time), and `Legend`, used across calculators. Two config-driven reusable calculators cover several tools each: `components/LoanCalculator.tsx` (auto/mortgage/student-loan — a `LoanConfig` of amount fields summed/subtracted into a principal, then `loanPayment`) and `components/GrowthCalculator.tsx` (compound/retirement — `growthSeries`, time by years or by ages). `components/RothIraCalculator.tsx` + `lib/rothIra.ts` is a dedicated Roth IRA calc (after-tax contributions growing tax-free, with a side-by-side comparison vs. a taxable account that loses return to a "tax drag"; the 2026 contribution limits `ROTH_LIMIT`/`ROTH_LIMIT_50PLUS` live in `lib/rothIra.ts` — update yearly). `components/PaycheckCalculator.tsx` reuses the `estimateTaxes` engine. The Budget Planner predates `CalcUI` and keeps its own copies.

The **budget calculator** (`components/BudgetCalculator.tsx`, a `"use client"` component) does live take-home estimation: salary (with a pay-period selector — annual/monthly/biweekly/weekly) or hourly income, a **before/after-taxes** toggle (post-tax skips estimation and hides the "Where you live" card), other income, filing status, and US state/city. All tax logic + data lives in `lib/taxData.ts` (`estimateTaxes()`) — real 2026 federal brackets/standard deductions + FICA (update yearly), and **simplified flat per-state approximations** (clearly labeled estimates, easy to swap for full brackets). Edit tax numbers there, not in the component. The plain-language budget verdict ("Whoa there…") and per-category "running high" flags come from `lib/budgetInsights.ts` (`getBudgetInsights()`), which compares each expense to rough benchmark shares of take-home — tune the thresholds there.

**Persistence / "pick up where you left off":** `lib/storage.ts` is an SSR-safe localStorage layer (`loadJSON`/`saveJSON`/`removeStored`, keyed by versioned `STORAGE_KEYS`). The quiz saves its result on reaching the results screen and offers a "Welcome back" resume on the welcome screen; the budget calculator hydrates from and persists to storage on every change (with a "Clear" control). **Read tracking (no login):** `lib/readTracking.ts` stores a slug→timestamp map under `STORAGE_KEYS.readArticles`. `components/MarkAsRead.tsx` (mounted on every article page) marks an article read at ~85% scroll of `#article-content` (8s dwell for articles that fit the viewport). Consumers: `components/QuestionStrip.tsx` (homepage "real question" list — read articles rotate out of a 12-question pool, all-read state links to /learn; REVERTED July 2026 to the original serif divided rows after the owner polled readers — the Base44 numbered-box iteration lives on as `components/QuestionBoxes.tsx`, mounted on /start-here as the "Already have a question? Skip ahead" band (owner-picked from mockups July 2026) — reusable on other LIGHT surfaces, but don't re-apply it to the homepage band), `components/WelcomeBack.tsx` (homepage strip: next unread article in the last-read topic, falling back to quiz-selected topics, hidden for first-time visitors), and `components/ReadBadge.tsx` (`ReadBadge` check chips on topic-hub roadmap cards + `TopicProgress` "You've read X of N"). All render nothing until mounted, so no hydration mismatch. This is intentionally the on-ramp for real accounts — see `docs/auth-plan.md` for the **deferred Supabase auth plan** (accounts stay optional, never gate content; localStorage snapshots sync into the account on first login). Don't add forced sign-up — the brand promises "no sign-up walls." **Accounts (July 2026, ships dark):** full Supabase password-auth + sync is BUILT and inert until `NEXT_PUBLIC_SUPABASE_URL`/`NEXT_PUBLIC_SUPABASE_ANON_KEY` exist — `lib/supabase.ts` (`accountsEnabled` gate), `/account` (`AccountPanel`: sign-up w/ email verification + 13+ checkbox, sign-in, forgot-password, signed-in profile editor with student/working/retired role + show-tag toggle), `/account/reset`, `lib/accountSync.ts` (login merge: MAP_KEYS union across devices, snapshots prefer local; then debounced live mirror via `setStorageMirror` hook in storage.ts — saveJSON is the single write path, keep it that way), `lib/profile.ts` (`empower:profile:v1` local mirror; `communityTag()` + `communityFlairs()` ride on Ask/Community Web3Forms payloads as `member_tag`/`member_flairs`). FLAIRS (July 2026): `FLAIR_OPTIONS` registry (16 options, kinds useful|fun, each with its OWN `color` — dark enough for tinted text on light cards and cream text on solid chips; `flairColor(id)`/`flairColorByLabel(label)` helpers, label lookup because community surfaces store labels), members may PICK any number but only the first `MAX_FLAIRS`=2 in pick-order DISPLAY anywhere (picker: showing pair renders solid forest, extra picks outlined, helper line says which two are live; unpicking a shown one promotes the next); stored in `profiles.flairs` jsonb (migration in docs/supabase-schema.sql); shown on the identity card, and on community posts/comments via optional `authorFlairs` labels on `CommunityPost`/`CommunityComment` (copy from the review email's member_flairs line when curating). Flair visibility follows the same show-my-tag toggle, `AccountButton` in Header, `/privacy`, `/api/keepalive` + vercel.json daily cron (free-tier anti-pause). Schema+RLS: `docs/supabase-schema.sql`; go-live steps: `docs/auth-plan.md`. Accounts stay optional forever — every feature must keep working logged-out. **Account OVERLAY (July 2026):** in-app links to /account open as an INTERCEPTED-ROUTE modal above the current page (app/@modal/(.)account + `modal` parallel slot rendered in the root layout; app/@modal/default.tsx returns null): the previous page stays visible behind a bg-ink/70 scrim, and X / Esc / scrim-click router.back() to exactly where you were (scroll preserved). Direct visits and auth-email redirects still render the full /account page (interception is client-nav only). `overlay` prop on AccountPanel/ProfileEditor skips the full-page section wrappers inside the modal. The overlay shell (components/AccountOverlayShell.tsx) is a direct child of <body> — position/zIndex INLINE (z 80, the body>* grain-rule gotcha). **Signed-in dashboard style zone (owner-picked July 2026, Kinetik-reference clone):** the /account SIGNED-IN view deliberately departs from bold-mix — it's a flat app frame (bg-paper-deep section — the owner removed the original bg-ink dark-green surround — → white rounded frame with a neutral ring-black/5 hairline → in-frame top bar + flat white sidebar) with white shadow-sm cards, sans-serif type (no font-display), forest as the single accent (solid forest buttons, forest tab-underline), hairline dividers (#eee7d9 via the DASH palette in AccountDashboard.tsx), and tabs INSIDE the content panel with a recent-reading status-chip table. WARMED UP July 2026 (owner: the flat clone read 'AI and stale'): the zone now uses the house voice in app-frame form — cream surfaces + sand hairlines instead of white/gray, font-display on titles/identity name, amber active-tab state (inset amber bar), ink-bordered frame with offset shadow, ghost TopicMark behind the forest identity card. Still an app frame (no sticker chips/tilts inside), but no more flat-white SaaS styling; DASH palette in AccountDashboard.tsx holds the zone's colors. Signed-in UI is verified via the dev harness at /dev/account-preview (stubbed session, 404s in production) + headless Chrome screenshots.

Learn-page copy (topic headlines, intros, related topics, tool cross-links) lives in `lib/learnContent.ts`, keyed by `TopicId`. `app/learn/[topic]/page.tsx` is the shared topic-hub template.

**Articles** (204 total; Investing 44, Budgeting 35, Credit 23, Government Aid 22, College 20, Home 18, Taxes 17, Protecting Your Money 13, Insurance 12 — the July 2026 owner directive was 200+, with the six additions chosen for genuine gaps, not padding: cosigning, balance transfers/consolidation, turning-26 health insurance, insurance-denial appeals, benefits cliffs, need-cash-fast). Thirteen of these are **roadmap articles** (pure routing pieces that sequence other guides via inline links, never re-teaching): registry in `lib/roadmaps.ts` (ROADMAP_SLUGS/ROADMAP_SET; every topic has ≥1). Consumers, all defensive: the /learn roadmaps section (`id="roadmaps"` — since July 2026 a featured forest banner for money-order-of-operations + card rows for the rest, replacing the old amber strip), a per-topic 'whole path, one page' banner + per-card Roadmap badges on /learn/[topic], a 'One stop on a longer path' banner on every article page (via `getRoadmapRefs()` in lib/articles/index.ts - SERVER-ONLY, importing it from a client component drags all articles into the bundle), 'Follow the roadmap' links under each Where-to-Start step on quiz results (server-derived `roadmapsByTopic` prop: quiz page -> QuizFlow -> QuizResults), the WelcomeBack strip's 'or follow the {topic} roadmap' link, and a pointer line under the homepage topic grid. New roadmap = write the routing article + add its slug to the registry live in `lib/articles/<topic>.ts` as `Article[]` (schema in `lib/articles/types.ts`), aggregated by `lib/articles/index.ts` — add a topic file to the imports + `topicArticleSets` array there. Each topic's articles can span multiple files (e.g. `credit.ts` + `creditExtra.ts`); they merge by `topicId` regardless of file split. Article bodies are structured `ArticleBlock[]` (`p`/`h2`/`h3`/`list`/`steps`/`tip`/`key`), rendered by `components/ArticleBody.tsx` at `/learn/[topic]/[article]` (SSG). The topic hub derives its Start-here/Go-deeper/Advanced roadmap from these articles via `getTopicRoadmap()` (falls back to the optional `learnContent.path` "Coming soon" only if a topic has zero articles; `guideCount()` likewise counts real articles first). **Edit article prose in `lib/articles/<topic>.ts`, not the page.** Voice is NerdWallet-style (see `budgeting.ts` as the reference). Inline markdown in `text`: `**bold**`, `*italic*`, and `[label](/internal-path)` links (internal paths only — rendered by `ArticleBody`'s `renderInline`); headers stay structural blocks. **Editorial conventions (from the July 2026 humanization pass):** at most ~3 em dashes per article (prefer periods/commas/colons/parentheses); no formula phrases ("Here's the thing", "The good news?", "It's not X — it's Y" inversions, pep-talk closers); each concept has ONE canonical owner article — other articles give it a sentence plus an inline link instead of re-explaining (e.g. withholding → taxes/how-to-fill-out-w4, scam red flags → money-safety/how-to-spot-a-scam, score factors → credit/what-hurts-your-score). Keep that dedupe discipline when adding articles. **State concrete current figures, not "check the current rate."** Articles name the actual dollar amounts (contribution limits, standard deduction, tax brackets, EITC/education-credit amounts, Pell max, federal loan limits/rates, etc.), tagged with the year (e.g. "For 2026, …") — the owner accepts the yearly-update burden. Do NOT add "confirm the current limit at IRS.gov"-style hedges: the July 2026 owner directive is that the numbers stand on their own (an "it rises most years" fact is fine; delegating the reader to look the number up is not). Current values are 2026 (IRS retirement/tax figures: 401(k) $24,500, IRA $7,500, HSA $4,400/$8,750), 2026 tax year (brackets/deduction $16,100/$32,200/EITC max $8,231), and 2026–27 (Pell $7,395, undergrad Direct Loan 6.52% — loan rates reset each July 1, Pell each award year). When figures genuinely vary by person/state/lender (your card's APR, insurance quotes, mortgage credit minimums, SNAP limits), give a representative anchor *and* keep the "check yours" guidance.

The article page is built for engagement: `components/ReadingProgress.tsx` (top scroll bar tracking `#article-content`), `components/ArticleToc.tsx` (scroll-spy ToC, pushed into the left margin on `xl` via the page's grid while the article column stays centered), and a drop cap on the first paragraph. Heading ids for the ToC come from `lib/articles/headings.ts` — `ArticleBody` and `extractHeadings` both call `makeHeadingIder()` in block order so the ids match. Glossary terms render as `components/GlossaryTerm.tsx` (a client popover showing the definition inline on hover/tap) instead of plain links — keeps readers on the page.

Article reading style: the **body is a reading serif** (Newsreader, `--font-reading`, applied via the `.article-body` / `.article-lead` classes in `globals.css`); headings/pull-quotes stay Lora (`font-display`). Each article is colored by its **topic accent** (`meta.color`): the page passes `accent` to `ArticleBody`/`ArticleToc`/`ReadingProgress` and sets `--article-accent` on `#article-content` (the drop cap reads it). That accent drives the heading marker rules, list bullets, step numbers, pull-quote border, emblem, and dot-grid graphic — so budgeting reads green, college gold, home-ownership terracotta, etc. Body text is intentionally near-black (high contrast) — don't reintroduce `/70`–`/80` opacity on article prose.

**Article knowledge checks:** an article may define `quiz` (2–4 multiple-choice questions; schema `ArticleQuizQuestion` in `lib/articles/types.ts`) rendered after the body by `components/ArticleQuiz.tsx` ("use client") — deliberately SMALL: one question at a time with a 'Question 1 of 3' counter and an Optional chip ('Skip it freely — nothing is graded'), answers lock on click with instant feedback + explanation and a Next-question button in the article accent; score persists to localStorage (`empower:article-quizzes:v1`). 18 flagship articles (2 per topic) have one. Other tools outside the calculator registry: the Letter Generator at /tools/letters (components/LetterGenerator.tsx — credit-dispute + debt-validation letters built in-browser on the CFPB sample structure; promoted via a card on the tools hub) and TermOfTheDay on /glossary (date-deterministic pick from the glossary). Questions must be answerable from that article alone; wrong options are the misconceptions the article corrects. Add a quiz when an article teaches a load-bearing concept; skip it for list/roundup articles. **Topic mini quizzes:** `lib/topicQuizzes.ts` holds 5-question topic-level quizzes for the six key topics (budgeting, credit, taxes, investing, college, money-safety - absence elsewhere is deliberate), served at `/learn/[topic]/quiz` (SSG from `TOPIC_QUIZ_IDS`, 404 for topics without one). Each question carries a `sourceSlug`; wrong answers render a 'Review: <guide>' link (resolved server-side). UX/persistence mirror ArticleQuiz (`components/TopicQuiz.tsx`, scores under `topic-quiz:<id>` in the same `empower:article-quizzes:v1` map). Every article of a quiz topic shows `components/TopicQuizCard.tsx` in its end-of-article suggestions, deliberately ABOVE the tool and roadmap cards. **Done things stop prompting (owner directive, July 2026):** end-of-article prompts self-hide after mount once the visitor has done them — the site-quiz band (`QuizPromo`), the tool card (`ToolCard`, hidden once that tool page was opened — `components/VisitTracker.tsx` in the layout records every `/tools/*` visit to `empower:visited-tools:v1`), roadmap card (`RoadmapPathCard`), and read-filtered `RelatedArticles` all live in `components/ArticleFollowUps.tsx`; `TopicQuizCard` hides once that topic quiz has a saved score. The follow-up stack sits in a `mt-10 space-y-4` container so the glossary note keeps its distance whichever cards survive. Topic hubs sink read guides to the bottom of each roadmap group via `components/ReadOrderedGrid.tsx` (stable within read/unread halves; ReadBadge stays the indicator), and the big Start-here banner demotes the same way: once its article is read, `HideWhenRead` hides the banner and the article surfaces as an `onlyWhenRead` compact card at the bottom of its group (skipped when the group has no other cards to join). **THE MEMORY CONTRACT (owner directive, July 2026 — apply to every new surface):** single-item prompts (quiz band, tool card, roadmap card/banner, mini-quiz card, WelcomeBack roadmap link, quiz-results roadmap links and course/challenge rec — the last skips earned badges via the `empower:course-badges/challenge-badges` maps) HIDE once done; ARTICLE LISTS never hide read items — they sink them to the bottom with a visible Read chip (RelatedArticles, topic-hub groups, /learn roadmaps strip, /life MomentReads, BlogList) and only disappear when everything in them is read. Course reading paths keep curriculum order (ReadBadge only). All render their default server-side (no hydration mismatch) and remove themselves client-side; keep this pattern for any new end-of-article prompt. Stems must not duplicate article-level `quiz` blocks or course finals.

Each article's "try it yourself" tool link comes from `lib/articleTools.ts` (keyed by article slug), falling back to the topic-level `learnContent[topic].tool`.

**Cross-calculator import:** `lib/calcImports.ts` reads another calculator's saved localStorage snapshot and returns the one useful number — `readDebtSummary()` (total monthly debt payment) and `readBudgetSummary()` (take-home / expenses / leftover, recomputed from the budget snapshot via `estimateTaxes`). Targets check availability on mount and show an import affordance: the Budget Planner imports debts as a "Debt payments" expense; the Debt Payoff and Savings Goal calculators import the budget's monthly leftover (as extra payment / monthly contribution). Imports are one-time on click (the value becomes an editable field).

**Glossary:** `lib/glossary.ts` holds terms + plain-English definitions (410 terms after the July 2026 article-sweep expansion; every key term the articles teach). Surfaces (term + aliases) must stay UNIQUE file-wide, and avoid bare generic words/aliases ("snowball", "claim", "options", "crypto", "equity" as an alias) — the auto-linker matches whole words case-insensitively across all 195 articles, so a generic surface mis-links metaphorical uses. Richer glossary also = richer auto-derived course flashcard decks. `ArticleBody` auto-links the FIRST occurrence of any glossary term/alias in each article to `/glossary#<slug>` (with a hover definition) — authors just write normal prose using standard terms, no manual tagging. `app/glossary/page.tsx` is the A–Z glossary. To make a new term link everywhere, add it to `lib/glossary.ts`. GLOSSARY FINDABILITY (owner ask, July 2026): a forest band on /learn (live term count + TermOfTheDay card), a spot in the SearchDialog empty-state Jump-to list (key pages before topics), and the community rail's Explore links — keep these when restyling those surfaces. Terms with a DEDICATED guide carry `article: "/learn/<topic>/<slug>"` (114 mapped July 2026): the popover then shows 'Read the full guide' (self-suppressed on that page via usePathname) and the /glossary page links it too. When writing a new article that owns a term, set its glossary entry's `article` field.

### The quiz (the only real logic in the app)

The quiz is the centerpiece. Its full product spec lives in `docs/quiz-spec.md` — **read it before changing quiz behavior, and update it in the same change**, since it's the source of truth for question wording, scoring rules, and results copy.

Data and logic are fully separated from rendering:

- **`lib/topics.ts`** — the 9 canonical financial topics (`TopicId` union, icons, colors, `/learn/*` hrefs). Adding a `TopicId` is a spine change: TypeScript forces matching entries in `learnContent`, and in `quizData.ts`'s `knowledgeCheckBank` / `topicWhyBlurbs` / `topicResourceCards` records (plus an optional Q3 option). Everything driven by `topics.map()` — homepage cards, Learn hub, nav dropdowns, static params — updates automatically. `getTopic(id)` throws on unknown ids. This list is the spine: homepage topic cards, quiz topics, knowledge-check banks, and resource cards all key off `TopicId`.
- **`lib/quizData.ts`** — all quiz content + pure scoring/results functions: question definitions (`quizQuestions`), the knowledge-check question bank (`knowledgeCheckBank`, keyed by `TopicId` → `Tier`), and result generators (`getFinancialProfile`, `getKnowledgeCheckFeedback`, `getWhereToStart`, `scoreKnowledgeCheck`, resource-card maps). Keep this file logic-only and presentation-free.
- **`components/QuizFlow.tsx`** (`"use client"`) — the state machine: `welcome → q1..q5 → kc → results`. Holds all answer state and drives navigation/progress. The knowledge check (`kc`) is built dynamically from Q3-selected topics at the difficulty `Tier` derived from Q5 (`confidenceTier`), pulling `KC_QUESTIONS_PER_TOPIC` (currently 2) questions per topic.
- **`components/QuizResults.tsx`** — pure presentation; takes computed answers/tier and calls the `lib/quizData.ts` functions to render the 4-part results page.

Key branching rules baked into the flow (see spec for rationale):
- **`not-sure`** on Q3 is **combinable** with real topics (plain toggle, not exclusive). If real topics are also chosen, the knowledge check runs on those topics as normal. If *only* `not-sure` is chosen, a short **general knowledge check** runs instead (`GENERAL_KC_PICKS` in `quizData.ts` — one beginner question each from a few foundational topics) and results use "let's figure it out together" framing with all topics for guidance. The knowledge check (topic or general) is also **skippable** mid-flow via a footer ("Skip this question" / "Skip to my results"); skipping sets `skippedKc`, which suppresses score feedback on the results page.
- Q5 confidence maps to a `Tier` (`beginner`/`advanced`) that selects which knowledge-check questions are asked *and* which resource-card variant is shown.
- The **confidence-mismatch** feedback messages (said-confident-but-scored-low, said-beginner-but-scored-well) are intentional and should not be removed — they're what makes the quiz feel honest.

## Styling conventions

- Tailwind v4 with the theme defined in `app/globals.css` via `@theme` (not a `tailwind.config`). Brand colors are CSS custom properties exposed as utilities. The palette is deliberately restrained — one confident green plus warm accents, **not** a rainbow:
  - `ink` (`#11211c`, near-black pine) — headings and body text. (It is no longer the background of dark *sections* — those are now `forest`; see below.)
  - `forest` (`#0c4a39`, deep green) is **the** brand green and the color of all dark chrome: the sticky header (`components/Header.tsx`, `bg-forest/95`), the footer (`components/Footer.tsx`), **every dark section/card on the homepage** (the "come with a real question" strip, mission pull-quote card, quiz promo card, tools band, community band gradient — all `bg-forest` / `from-forest…`), and the single green across all calculators (charts, callouts, positive numbers). Raised surfaces *on* green (cards, inputs) use `forest-700` (`#15624b`); hairlines on green use `border-white/10`. `emerald` (`#1f9069`) remains in the theme for incidental accents but was retired from the calculators so the green reads as one color. If you need a second green in a chart, pair forest with a non-green (stone/amber/terracotta), as the donuts do.
  - `amber` (`#e7a33c`) — primary CTAs/highlights — with `amber-deep` for hover/pressed.
  - `terracotta` — energetic accent, used sparingly (eyebrow dots, etc.).
  - `paper` (`#f7f2e8`, warm cream) page background, `paper-deep` for alternating sections, `sand` for warm borders/dividers, `cream` (near-white) for cards and text-on-dark, `stone` for muted secondary text on light.
- **Design direction (July 2026): "bold mix" on a warm-editorial base.** Three coexisting voices, each with a role (utilities + rationale in `globals.css`): **C editorial-maximal** for page heroes (forest field, huge Lora at `text-5xl sm:text-7xl+` `font-medium leading-[0.95]`, one phrase `italic text-amber`, a ghost TopicMark/doodle at `opacity-[0.07]` bleeding off a corner) · **A color fields** for section rhythm (full-bleed solid `bg-amber text-ink`, forest, or terracotta bands; every page gets at least one) · **B ink-&-shadow** for touchable objects (`card-ink`/`card-ink-lg`/`btn-ink`: 2px ink borders + hard offset shadows + hover lift; a minority of grid items tilt `lg:rotate-[±0.5deg]`; sticker chips `-rotate-2`). Rules: ink shadows only on light surfaces (dark surfaces may use a colored shadow like `shadow-[7px_7px_0_#e7a33c]` sparingly); every page mixes ≥2 voices. **HOMEPAGE FREEZE LIFTED (owner directive, July 12 2026): the homepage may now be redesigned like any other page** — the owner is soliciting external design-audit proposals and will approve changes; all house rules (no AI-template tells, real data only, no gradient washes over photos) still apply there. Its owner-approved final state: original light hero (squiggle headline, photo), forest question band + the question Ticker, gradient-photo quiz promo card (gradient washes are allowed HERE only — grandfathered by owner choice), B-voice "Run your own numbers" section, and the community band = original photo/gradient layout with the italic-amber "Community" and the two buttons (Visit the community / Join a challenge). Owner-approved additions since the freeze (do not remove): the roadmaps pointer line under the topic-card grid (July 2026, owner asked for roadmaps on the homepage) and the WelcomeBack strip's roadmap link. Real photography, asymmetric layouts, and honest stats remain the base; still avoid the "flat AI" tells (blob SVGs, rainbow uniformity, perfect symmetry).
- **Typography:** headlines use the Lora serif via the `font-display` class (defined in `globals.css`, backed by `--font-logo` from `app/layout.tsx`). Body/UI uses Geist sans (the default). Reach for `font-display` on any hero/section headline. **Gotcha:** the `next/font` variable classes MUST stay on `<html>` (not `<body>`) — the `@theme` block in `globals.css` references `--font-logo`/`--font-geist-*` at `:root`, and a `:root`-level `var()` can't see body-level variables; with them on `<body>` every font silently falls back to system sans site-wide (this happened once — headings/articles all rendered sans and nothing errored).
- **Imagery:** real photos live in `public/images/` (sourced from Unsplash; free license), used for **people/hero** shots — rendered with `next/image` (`fill` inside aspect-ratio containers). Portraits `portrait-1..4`, plus `students-collab`, `community`, `graduate`, `classroom`, `studying`, `mentor`. **Topic marks are hand-drawn inline SVGs** in `components/TopicMark.tsx` (one geometric mark per `TopicId` — gauge, pie, %, mortarboard, rising line, house, life ring, shield-check, umbrella — drawn in the topic's accent color, same design family as the homepage tool doodles). Used on the homepage topic grid, `/learn` hub, topic-hub heroes, "Keep going" cards, and the quiz. The old PNG illustration pipeline (`public/images/illustrations/`, `scripts/build-illustrations.mjs`) was deleted July 2026 because the PNGs' internal padding made them illegible at badge sizes — do not reintroduce it; edit the SVG paths in TopicMark instead. (`topic.image` photos in `public/images/topics/` are separate and still used on topic cards/heroes.)
- **No "AI-template" tells:** the design deliberately avoids the generic AI-site signatures — no "eyebrow pill" capsule labels (use a plain `text-xs uppercase tracking-[0.2em]` kicker instead), no Lucide-icon-in-colored-rounded-square tiles on cards, no decorative watermark icons or gradient hero washes, no emoji in UI copy. Also banned (removed in the second de-AI pass, benchmarked against slug-mart.com as the anti-pattern): slogans dressed as stats ("100% free / 0 sign-ups" number bands), floating/animated badge cards over hero photos, fake social-proof facepiles, staccato triplet headlines with hand-drawn squiggle underlines, placeholder "Coming Soon" content, forms that don't submit anywhere (the old newsletter), `rounded-full` pill CTAs (use `rounded-md`), and an arrow → on every link (links are underlined text: `underline decoration-amber decoration-2 underline-offset-4`). Homepage content must be REAL data pulled from the libs (live guide counts, actual calculators from `toolsRegistry`, real Q&As from `communityQuestions`) — never invented numbers or placeholder cards. Lead with typography, whitespace, the illustrations, and real photos.
- **Behavioral motion (July 2026, all reduced-motion safe):** `components/AnimatedNumber.tsx` rolls calculator headline results to new values (wired into 11 calculators + Reality Check; format fn passed in); `components/Charts.tsx` Donut/TrendChart draw themselves in once on first viewport entry; `components/BadgeBurst.tsx` fires a one-shot medal pop + brand-color confetti the moment a course/challenge badge is FIRST earned (wired in CourseQuiz + ChallengeChecklist; keyframes in globals.css next to ticker-scroll); `components/CompoundStory.tsx` is the scrollytelling interlude on the magic-of-compound-interest article ONLY (sticky chart + IO-driven steps, real Maya/Leo math computed in-component: ~$281k vs ~$244k). NOTE: the preview panel throttles requestAnimationFrame — rAF animations snap there but run at 60fps in real browsers; verify motion with headless puppeteer, not the preview.
- **Motion:** `components/Reveal.tsx` is the one scroll-animation primitive — a fade-and-rise on first viewport entry (IntersectionObserver; `delay` staggers siblings; content is server-rendered visible and only hidden on mount when JS runs, and the global reduced-motion rule neutralizes it). Used on hub grids and homepage card grids. Don't add other animation systems.
- **Pastel tiles:** the flat pastel product-tile language (backgrounds/accents in `toolStyles`, category doodles in `components/ToolDoodle.tsx`, and PER-TOOL marks in `components/ToolMark.tsx` — one hand-coded flat doodle per calculator slug, drawn in the category accent; add a case when adding a calculator) is the house style for category/feature tiles — used on the /tools hub (restructured July 2026 to the Base44 skeleton in OUR skin, owner call: hero + 'start there' line, standalone By-category jump-tile section, then per-category All-calculators sections where the `main` tool gets a hero card beside a 2-col sibling grid — no more left-border category banners) (the homepage tools bento was replaced July 2026 by the Base44-copied “math, visualized” compound-chart band: components/CompoundChart.tsx + the mathCalcs rail in app/page.tsx, wavy cream divider on top), and (as per-item color tints) course/challenge cards. (The /learn topic-launcher tiles were removed July 2026 when /learn was restructured to the Base44 skeleton in OUR skin, owner call: light squiggle hero + quiz link, 'full library' horizontal topic rows with alternating photo sides / ghost counts / Start-with links, a 'Start here' band of four starter guides (START_HERE_SLUGS in app/learn/page.tsx), roadmaps moved LOW with the featured banner, glossary band unchanged.)
- **CSS gotcha (Turbopack/Lightning CSS):** rules placed AFTER the `.article-dropcap::first-letter` block in `globals.css` were silently dropped from the compiled stylesheet in dev (discovered July 2026 when the ticker keyframes vanished). The dead zone starts EARLIER than the dropcap block (badge keyframes placed at ~line 138, above dropcap at 152, were still dropped July 2026): put new global CSS/keyframes immediately after the ticker-scroll keyframes block, then verify via document.styleSheets scan that the rule is actually served.
- **Ticker:** `components/Ticker.tsx` is a slow marquee strip (ink-bordered forest band, serif items with amber sparkle separators) fed real question links; CSS in globals (`ticker-scroll`), pauses on hover, stops under reduced motion. Used on the homepage under the questions band and on /glossary (terms). **Owner taste rule (July 2026): do NOT replicate the ticker (or any signature element) onto more pages — three lookalike marquees were added and reverted as 'the same thing repeated.' A signature element stays special by staying rare.**
- **Gradient washes over photos are banned** (a SlugMart-class AI tell). The community band is a split panel (solid forest card + ink-framed tilted photo) and the quiz promo card is solid amber with a ghost TopicMark — keep it that way. Per-card arrow → links were removed sitewide in the same pass (underlined colored text instead); metadata pill-chips (clock icons in capsules) were flattened to plain text.
- A faint SVG paper-grain overlay is applied via `body::before` in `globals.css` to avoid a flat vector look; real content sits above it via `body > * { z-index: 2 }`. **Gotcha:** that `body > *` rule is *unlayered* CSS, so it beats Tailwind's layered `.fixed`/`.z-*` utilities (cascade layers lose to unlayered regardless of specificity). Any direct child of `<body>` — including a `createPortal` target like `components/MobileNav.tsx`'s drawer or a future modal — must set `position`/`z-index` via **inline style**, not utility classes, or it gets pinned to `position: relative; z-index: 2`.
- **Primary nav** lives in `lib/nav.ts` (the shared `nav` array, built from `topics` + `toolCategories`) — used by both the desktop header and the mobile drawer, so adding a topic/category updates both.
- **Header dropdowns:** `components/Header.tsx` (server component) renders entries with `items` as hover/focus dropdown menus (Learn → the 9 topics, Tools → the 4 categories, Start Here / Resources / About → sub-links). It's **CSS-only** (`group-hover` + `group-focus-within`, no JS) — the panel uses a `pt-3` bridge so hover isn't lost crossing the gap, and is `absolute` (not `fixed`) so the header's `backdrop-filter` containing block doesn't break it.
- **Mobile nav:** the header nav is desktop-only (`hidden xl:flex` since July 2026; July 13 owner pass: Courses merged INTO the Learn dropdown footer and For Students sits after Tools — seven top-level items, still xl because seven remains tight below 1280; the drawer covers lg widths); `components/MobileNav.tsx` (`"use client"`) is the hamburger + full-screen drawer below `lg`, portalled to `<body>` (to escape the header's `backdrop-filter` containing block), with scroll-lock, Escape-to-close, and **expandable sections** mirroring the desktop dropdowns (tap a label to navigate, tap its chevron to expand the sub-items). The drawer scrolls (`overflow-y-auto`) with the "Take the Quiz" CTA pinned.
- **Fuzzy search everywhere (July 2026):** `lib/fuzzy.ts` is the shared typo-tolerance engine (normalize/tokensOf, capped Damerau–Levenshtein `editDistance`, `tokenMatch` = exact 1.0 / prefix 0.9 / substring 0.7 / 1-edit@len4+ 0.85→0.6 / 2-edits@len7+ / misspelled-prefix 0.55, `bestTokenScore`, `fuzzyScore` requiring all tokens for short queries else 60%). Wired into ALL search surfaces — SearchDialog's scoreItem (every token must land fuzzily; title hits weighted), lib/guide.ts score() (chatbot retrieval + /api/chat inherit it), the community post search, and the channel finder. 'buget calulator'→Budget Planner, 'FASFA'→FAFSA, 'crdit socre'→credit score, 'realty check'→Reality Check are the canonical test cases. Any NEW search feature must use lib/fuzzy.ts, not raw includes(). **Site search (⌘K):** `components/SearchDialog.tsx` (`"use client"`) is a command-palette search in the header (visible on desktop + mobile; opens on click or ⌘K/Ctrl+K). It's fully client-side over a **lightweight index built server-side** by `lib/search.ts` `getSearchItems()` — which maps articles → title/dek/href only (no bodies), plus calculators (`toolsRegistry`), topics, glossary terms, and key pages. The server `Header` calls `getSearchItems()` and passes the array as a prop, so the client bundle never imports `lib/articles`. It's a **full-width drop-down panel** (NerdWallet style): anchored directly under the sticky header (its `top` is measured from `document.querySelector("header")`'s height on open, so it tracks the header) and spanning the page width, with the search field, results, and footer in a centered `max-w-3xl` column over a full-bleed `bg-cream`. A `bg-ink/40` scrim dims everything below the header — but there is **deliberately no scroll-lock**: the scrim doesn't capture wheel events, so the page keeps scrolling behind the panel (the panel stays fixed under the header). Results are grouped into sections by kind (Topics/Calculators/Guides/Glossary/Pages — section order follows relevance, since groups are emitted in first-appearance order of the scored list) with a per-kind icon + color chip, the active row highlighted with an enter affordance, and a keyboard-hint footer. Empty state shows a "Jump to" group (topics + key pages). Portalled to `<body>` (scrim + panel) with **inline** `position:fixed` + `zIndex` 69/70 (the unlayered `body > *` rule would otherwise pin them — same gotcha as MobileNav); closes on Escape, an outside `mousedown`, or a scrim click; arrow/Enter/Esc keyboard nav. To make something findable, just ensure it's in `getSearchItems()`.
- **"Money Guide" chatbot:** `components/ChatLauncher.tsx` (`"use client"`) is a floating "Ask" launcher → chat panel, mounted once globally in `app/layout.tsx` (server, passing `getSearchItems()`). AI is ON (July 2026): `AI_ENDPOINT = "/api/chat"` and the route was upgraded from title-only grounding to CONTENT-GROUNDED RAG — it pulls the actual body text of the top ~3 article/blog matches (excerptFor(), ~1800 chars each) plus pointer titles into the prompt, carries the last 6 chat turns for follow-ups (history rides the POST; retrieval query = question + recent user turns), answers in the older-sibling voice (claude-haiku-4-5, plain text, names the single best article; scam-in-progress and crisis nudges built into the system prompt), and returns deep-grounded hrefs first in sourceHrefs. Shares ANTHROPIC_API_KEY with the comment-review route; without it the route 503s and the client falls back to the retrieval guide. Historically it was a **hybrid**: retrieval-only via `lib/guide.ts` `guideAnswer()` (soft, stopword-stripped matching over the same index — it never invents answers, just replies with a friendly line + the best matching guides/tools/resources, with a safety nudge to Resources on sensitive queries). The seam to **flip on real AI** is the `AI_ENDPOINT` constant in `ChatLauncher.tsx`. The backend is already sketched at **`app/api/chat/route.ts`** — a Node route handler that retrieves the top matches via `rankItems`, asks Claude (Haiku by default) to answer **using only that context** (guardrails: short, no individualized legal/tax/immigration advice, no invented numbers, routes to free help), and returns `{reply, sourceHrefs[]}`. It's **inert until `ANTHROPIC_API_KEY` is set** (returns 503 → client falls back to the guide). Turn-on is 3 steps in **`docs/ai-chat-setup.md`** (get key → set env var → set `AI_ENDPOINT = "/api/chat"`). If the API errors/caps out, the chat **falls back to the retrieval guide** so it never breaks. Needs a server host (Vercel/Node), not a pure static export. Portalled to `<body>` with inline `zIndex:55` (below the mobile drawer/search), non-modal (closes on Esc/outside-click, doesn't lock the page). z-index ladder: search 70 > mobile drawer 60 > chat 55 > sticky header 50.
- Use the `@/*` import alias (maps to repo root) for cross-module imports, e.g. `@/components/Header`, `@/lib/topics`.
- Icons come from `lucide-react`. Topic/tool objects carry their `icon` as a component reference and render it as `<topic.icon ... />`. Per-topic accent colors in `lib/topics.ts` are coordinated greens/amber/terracotta (inline hex), not clashing hues.
- Pages compose `<Header />` + content + `<Footer />` inside a `min-h-screen bg-paper text-ink` wrapper. Only interactive components are `"use client"`; pages stay server components.
